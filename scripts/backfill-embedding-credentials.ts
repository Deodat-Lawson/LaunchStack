/**
 * Moved to apps/web/scripts/backfill-embedding-credentials.ts.
 *
 * Run with:
 *   pnpm db:backfill:embedding-credentials
 */

console.error(
  "This script moved to apps/web/scripts/backfill-embedding-credentials.ts.\n" +
    "Run: pnpm db:backfill:embedding-credentials",
);
process.exit(1);
