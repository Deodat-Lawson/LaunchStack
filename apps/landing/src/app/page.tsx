// Placeholder. Replaced by apps/web/src/app/page.tsx in the move commit; this
// exists only so the scaffold commit builds a routable app on its own.
export default function Placeholder() {
  return null;
}
